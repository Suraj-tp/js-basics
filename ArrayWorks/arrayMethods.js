var nums=[
    10,20,
    [10,
    [10,20]
    ],
    [[10,20],
    [10,20,
    [1,2,3]]
    ]
]
console.log(nums.flat(3));
// sort()
// filter() =>
// reduce=> sum,min,max
// includes,some=> searching for an element
// flat
// forEach
// num.flat().forEach(num=>console.log(num))

// map,filter,reduce,find,some,forEach,flat,sort,includes

var num=[1,2,3]
num.pop()
console.log(num);